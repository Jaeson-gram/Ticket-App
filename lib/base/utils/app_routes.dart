class AppRoutes {
  // we don't have to tie them to any names, we will be tying them to their respective pages in the main.dart 'route' property.

  static const homePage = "/";
  static const allTicketsPage = ""; // maybe "all_tickets", just for... understandability. if that's a thing.
}
